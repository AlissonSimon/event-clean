spring:
  application:
    name: event-clean
  datasource:
    url: jdbc:postgresql://localhost:5433/event-clean
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080
